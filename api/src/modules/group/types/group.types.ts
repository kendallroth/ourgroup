/** Group member invitation status */
export enum GroupInvitationStatus {
  ACCEPTED = "accepted",
  PENDING = "pending",
  REJECTED = "rejected",
}

/** Group application status */
export enum GroupApplicationStatus {
  APPROVED = "approved",
  PENDING = "pending",
  REJECTED = "rejected",
}

/** Group member role */
export enum GroupMemberRole {
  ADMIN = "admin",
  MANAGER = "manager",
  MEMBER = "member",
}
